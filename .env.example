## Copy this file to `.env` and fill values for local testing.
# Do NOT commit your actual service account key file or credentials.

GCS_BUCKET=
# If running locally with a service account key file:
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

PORT=3000
